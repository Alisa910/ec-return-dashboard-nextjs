# 🚀 快速开始指南

欢迎使用 EC 退货率分析Dashboard（Next.js 版）！

---

## ⚡ 3步启动

### 第1步：安装依赖

打开终端，进入项目目录：

```bash
cd ec-return-dashboard-nextjs
npm install
```

### 第2步：启动开发服务器

```bash
npm run dev
```

### 第3步：访问应用

浏览器自动打开：**http://localhost:3000**

---

## 📊 查看效果

应用启动后，你会看到：

1. **首页**：6个渠道卡片（天猫、京东、抖音等）
2. **品牌统计**：MLB、MLB KIDS、Discovery 概览
3. **风险标识**：红色=高风险，橙色=观察，绿色=正常
4. **点击任意渠道卡片** → 查看店铺明细

---

## 🔄 使用真实数据

### 方法1：替换数据文件

```bash
# 将你的数据文件放到 public/data/ 目录
cp /path/to/your/data.csv public/data/sample_data.csv

# 刷新浏览器（Ctrl + R）
```

### 方法2：修改配置

编辑 `lib/config.ts`：

```typescript
export const DATA_FILE_PATH = '/data/你的文件名.csv'
```

---

## 📋 数据格式要求

CSV 文件必须包含这5个字段：

```csv
Year,Channel,Shop_Name,Return_Rate_YTD,Net_Sales
2024,天猫旗舰店,MLB官方旗舰店,5.2,12500000
2025,天猫旗舰店,MLB官方旗舰店,4.8,14250000
```

---

## 🚢 部署到 Vercel

### 方法1：通过 GitHub（推荐）

```bash
# 1. 推送到 GitHub
git init
git add .
git commit -m "feat: EC退货率Dashboard"
git remote add origin https://github.com/你的用户名/ec-return-dashboard.git
git push -u origin main

# 2. 访问 vercel.com
# 3. 导入 GitHub 仓库
# 4. 点击 Deploy

# 5. 获得 URL: https://你的项目名.vercel.app
```

### 方法2：通过 CLI

```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 登录并部署
vercel

# 3. 生产部署
vercel --prod
```

---

## 🛠️ 常用命令

```bash
# 开发模式
npm run dev

# 生产构建
npm run build

# 启动生产服务器
npm start

# 代码检查
npm run lint

# 类型检查
npm run type-check
```

---

## 📚 查看完整文档

- 📖 [README.md](README.md) - 完整项目文档
- 🚀 [DEPLOYMENT.md](DEPLOYMENT.md) - Vercel 部署详解

---

## 🎯 项目特点

### ✅ 技术优势

- **Next.js 14**：最新 App Router，性能优异
- **TypeScript**：类型安全，开发体验好
- **TailwindCSS**：快速开发，响应式设计
- **Vercel 原生支持**：一键部署，全球 CDN

### ✅ vs Python 版本

| 特性 | Next.js 版 | Streamlit 版 |
|------|-----------|-------------|
| 部署平台 | ✅ Vercel（推荐） | Streamlit Cloud |
| 性能 | ⚡ 极快 | 🐢 中等 |
| 定制性 | 🎨 高 | 有限 |
| 企业级 | ✅ 适合 | ⚠️ 有限 |
| 学习曲线 | 中等 | 简单 |

---

## ❓ 常见问题

### Q1: 依赖安装失败

```bash
# 清除缓存
rm -rf node_modules package-lock.json
npm install
```

### Q2: 端口 3000 被占用

```bash
# 使用其他端口
PORT=3001 npm run dev
```

### Q3: 数据加载失败

- 确认 CSV 文件在 `public/data/` 目录
- 路径为 `/data/filename.csv`（不含 `public`）

### Q4: 自定义品牌映射

编辑 `lib/config.ts`：

```typescript
export const BRAND_MAPPING = {
  'MLB': ['MLB', 'MM', 'ML'],
  'MLB KIDS': ['MK', 'MLBKIDS'],
  'Discovery': ['DX'],
  '你的品牌': ['关键词1', '关键词2']  // 添加这行
}
```

---

## 🎉 恭喜！

你已经完成了 Dashboard 的本地部署。

**下一步：**
1. 测试所有功能
2. 替换真实数据
3. 部署到 Vercel
4. 分享给团队成员

**需要帮助？**
- 查看完整文档：[README.md](README.md)
- 部署指南：[DEPLOYMENT.md](DEPLOYMENT.md)

---

**祝你使用愉快！** 🚀
